extends RefCounted
class_name IUserGlyph

var color := Color.WHITE # Glyph color
var vscale := 1.0 # Visual Scale [READ ONLY]

var current_layer := -1 # Glyph Layer [READ ONLY]
var layer_strings := [] # Layer String data
var layer_colors := [] # Layer Color data
